export interface Student {
    id: string;
    fullName: string;
    email: string;
}

export interface StudentDetailResponse {
    id: string,
    firstMidName: string,
    lastName: string,
    email: string,
    enrollmentDate: string
}